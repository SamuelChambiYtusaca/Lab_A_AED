#include <iostream>
#include "Circle_List.h"
using namespace std;

int main(){
    Circle_List<int> L1;
    L1.push_back(64);
    L1.push_back(3);
    L1.push_back(361);
    L1.push_back(84);
    return 1; 
}
